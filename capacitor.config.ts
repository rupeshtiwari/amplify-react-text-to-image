import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stabled.app',
  appName: 'amplifyapp',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
